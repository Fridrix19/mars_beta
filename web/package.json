{
  "name": "marscap-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "migrate": "node scripts/migrate.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "pg": "^8.23.0",
    "vue": "^3.5.43"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1"
  }
}
